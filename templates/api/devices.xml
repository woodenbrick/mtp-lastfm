<?xml version="1.0" encoding="UTF-8"?>
<mtp-lastfm status="{{status}}">
    {% ifequal status "OK" %}
    <devices>
    {% for device in devices %}
    <device>
      <manufacturer>{{device.manufacturer}}</manufacturer>
      <model>{{device.model}}</model>
      <users>{{device.user_count}}</users>
      <issues>{{device.not_working_count}}</issues>
    </device>
    {% endfor %}
  </devices>
  {% else %}
  <error>No devices were found on the server.</error>
  {% endifequal %}
</mtp-lastfm>
